This bot has no local browser (no-root, deps not in image). The in-build
visual gate is DEFERRED to post-deploy (screenshot the live *.vercel.app).
See web-coding-os Playbook 4. Remove this file once the image ships a browser.
